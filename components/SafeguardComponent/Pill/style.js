import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  pill: {
    borderRadius: 8,
    paddingHorizontal: 15,
    paddingVertical: 10,
    // backgroundColor: "#ccc",
    backgroundColor: "#F3F4F6",
  },
  pillText: { fontSize: 12, color: "#333", fontFamily: "Jost-SemiBold" },
});

export default styles;
